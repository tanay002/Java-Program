package com.objectclass;

public class HashCodeMeth
{
    int i;
	 String j;
	 
	 public HashCodeMeth(int i,String j) {
		i=this.i;
		j=this.j;
	}
public static void main(String []args)
{
	HashCodeMeth h=new HashCodeMeth(1,"abc");
	HashCodeMeth h1=new HashCodeMeth(1,"abc");
	int k=h.hashCode();
	int k1=h1.hashCode();
	System.out.println(k);
	System.out.println(k1);
}

}

/*1.Hashcode method does not return address of object
 2. for each object,JVM generate a unique integer number 
 which is hashcode.
 3.
 when we call hashcode method,it convert the internal 
 address of object into integer number by using an algo.
 
 4.Different integer no  
 */